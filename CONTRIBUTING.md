# ร่วมพัฒนา

ระบบนี้เขียนขึ้นที่โรงพยาบาลชุมชนแห่งหนึ่งและใช้งานจริงกับผู้ป่วยอยู่
ข้อเสนอแนะและ pull request ยินดีรับทุกรูปแบบ โดยเฉพาะจากโรงพยาบาลที่ HOSxP
ตั้งค่าไว้ไม่เหมือนกัน เพราะนั่นคือสิ่งที่ต้นทางมองไม่เห็น

## กติกาที่ห้ามละเมิด

ข้อเหล่านี้ไม่ใช่สไตล์การเขียนโค้ด แต่เป็นเหตุผลที่ระบบนี้ได้รับอนุญาตให้ต่อกับฐานข้อมูลจริง
PR ที่ละเมิดจะถูกปฏิเสธแม้จะแก้ปัญหาได้ก็ตาม

1. **ห้ามเขียนอะไรลงฐาน HOSxP** ไม่ใส่ trigger ไม่สร้างตาราง ไม่อัปเดต flag ไม่ทำ
   schema migration ใด ๆ กับตารางของผู้ขาย สถานะทั้งหมดเก็บในฐานของแอปเอง
2. **ห้ามเอา HOSxP มาเป็น connection ใน Lucid** เพราะจะมี migration runner
   และ model ที่ `save()` ได้ชี้ไปที่ฐานโรงพยาบาลทันที พลาดครั้งเดียวก็เสียหาย
   ใช้ `HosxpClient` เท่านั้น
3. **ห้ามให้เลขบัตรประชาชนออกไปนอกระบบ** ไม่เข้ากลุ่ม LINE ไม่ลง log
   ไม่เก็บในฐานของแอป และห้ามถอด `scrubCid()` / `scrubDeep()` ออกไม่ว่าด้วยเหตุผลใด
4. **ห้ามทำให้ poller ล้ม** งานแจ้งเตือนที่ผู้ป่วยรออยู่ต้องเดินต่อเสมอ
   ส่วนประกอบอื่นพังให้ log ทิ้งแล้วไปต่อ
5. **ห้าม commit ความลับ** `.env` · `docs/reference/` · `docs/hosxp-schema.md`
   ถูก ignore ไว้แล้ว อย่าเอาออก และอย่าเอา IP ชื่อเครื่อง หรือ hash รหัสผ่าน
   ของโรงพยาบาลตัวเองใส่ลงโค้ดหรือเอกสาร ใช้ช่วง `192.0.2.0/24` ซึ่งสงวนไว้
   สำหรับตัวอย่างในเอกสารโดยเฉพาะ
6. **ห้ามส่งข้อความจริงตอนเทสต์** อย่าสร้างเคส 506 ปลอมในฐานโรงพยาบาล
   ใช้ `cdcu:check --demo` และ dry run แทน

## ก่อนส่ง PR

```bash
docker compose exec web npm run typecheck
```

```bash
docker compose exec web npm run test
```

```bash
docker compose exec web npm run lint
```

## เรื่องที่ต่างกันในแต่ละโรงพยาบาล

ถ้าเจอว่าโค้ดสมมติเอาเองว่าฐานของคุณต้องเป็นแบบใดแบบหนึ่ง **นั่นคือบั๊ก** เปิด issue มาได้
ที่รู้อยู่แล้วและอยากได้คนช่วย

- ตารางคิวถูกกำหนดเป็น `ovst_queue_server` ตายตัวใน `queue_poller.ts`
  ควรทำให้เลือกได้จากหน้าเว็บ
- ชุดข้อมูลตัวอย่างใน `notify:seed-examples` อิงรหัสแผนกและพฤติกรรมของฐานต้นทาง
  เช่น `cur_dep = '999'` แปลว่ากลับบ้าน และ X-Ray อยู่ที่ `xray_head` ไม่ใช่ `xrayexam`
- `db-sync` สมมติว่าทำ replication แบบหลายเครื่อง ที่ที่มีเครื่องเดียวควรซ่อนหน้านี้ไป

## ภาษา

คอมเมนต์ เอกสาร และข้อความบนหน้าเว็บเป็นภาษาไทย เพราะคนที่ต้องดูแลต่อคือเจ้าหน้าที่ไอที
โรงพยาบาล ไม่ใช่โปรแกรมเมอร์ต่างชาติ ส่วนชื่อตัวแปรและชื่อฟังก์ชันเป็นภาษาอังกฤษตามปกติ

คอมเมนต์เขียนเพื่อบอก**ว่าทำไม** ไม่ใช่บอกว่าโค้ดบรรทัดถัดไปทำอะไร
