# CHANGELOG

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/th/1.1.0/)
เลขรุ่นตาม [Semantic Versioning](https://semver.org/lang/th/)

วันที่เขียนเป็น พ.ศ. ให้ตรงกับที่ใช้ในระบบ

## [ยังไม่ออกรุ่น]

## [0.5.3] — 24 ก.ย. 2569

เลขรุ่นตรงกับ image ที่รันอยู่ที่โรงพยาบาลต้นทาง ระหว่าง 0.1.0 ถึงรุ่นนี้เป็น image
ที่ build ใช้ภายในแล้วไม่ได้ตัดรุ่นใน repo จึงรวมทุกอย่างไว้ในหัวข้อนี้

### เพิ่ม

- **แจ้งเตือนผู้ป่วยกลุ่มประคับประคอง (Palliative Care)** — คัดจากรหัสวินิจฉัยใน
  `ovstdiag`/`iptdiag` ตามเกณฑ์ 9 กลุ่มโรค (มะเร็ง C00-C96 D37-D48 · ระบบประสาท
  I60-I69 F03 · ไตวาย N18.5 · ถุงลมโป่งพอง J44 · หัวใจล้มเหลว I50 · ตับล้มเหลว K72
  K70.4 K71.7 · เอดส์ B20-B24 ยกเว้น B23.0 B23.1 · ผู้สูงอายุ R54 · เด็ก Q89 P07)
  แจ้งได้ทั้งทันทีที่พบและสรุปรายวัน หน้าตั้งค่าที่ `/notify/pc` คำสั่ง `pc:check`

  กันซ้ำด้วย **HN** ไม่ใช่ vn — โรคกลุ่มนี้เรื้อรัง ผู้ป่วยคนเดิมมาปีละหลายสิบครั้ง
  ถ้าแจ้งทุกครั้งที่เจอรหัสกลุ่มจะถูกน้ำท่วม ผู้ป่วยที่เข้าเกณฑ์หลายโรคนับเป็นคนเดียว
  อยู่กลุ่มที่มาก่อนในลำดับ (มะเร็งก่อน) ต้อง "รับทราบผู้ป่วยเดิม" ลง `pc_seen`
  ก่อนเปิดใช้ ไม่งั้นรอบแรกจะยิงผู้ป่วยทั้งฐานราว 1,600 รายออกไปทีเดียว

- **กวาดการมาโรงพยาบาลของผู้ป่วยประคับประคองลง `pc_visits`** ทุก 5 นาที
  พร้อมเช็ก `patient.death` เติม `pc_seen.death_on` — ให้ระบบติดตาม (bpfollow)
  ซึ่งไม่ต่อ HOSxP ใช้แจ้งว่า "คนไข้มา รพ. วันนี้" และนับว่ามาครั้งที่เท่าไหร่ตั้งแต่
  รับเข้า ทะเบียนที่กวาดรวม `bp_pc_patients` ที่ทีมเพิ่มเองในระบบติดตามด้วย
  (อ่านอย่างเดียว) ทำงานไม่ขึ้นกับว่าเปิดแจ้ง LINE หรือไม่ `pc:check --visits`

- **รายงานความดันโลหิตสูงเข้ากลุ่มเจ้าหน้าที่** — อ่านผลคัดกรองแรกรับจาก `opdscreen`
  ของ HOSxP แล้วส่งรายที่ความดันเกินเกณฑ์เข้ากลุ่ม LINE **วันละครั้งตามเวลาที่ตั้ง**
  (ตั้งต้น 15.00 น.) พร้อมหน้าตั้งค่าที่ `/notify/vitals` ปุ่มสั่งส่งเดี๋ยวนี้
  และคำสั่ง `vitals:check` สำหรับดูข้อความก่อนเปิดใช้ ตั้งต้นปิดไว้

  ข้อความมี **HN · ชื่อ-สกุล · ที่อยู่ · เบอร์โทร · สถานพยาบาลรอง** (`vn_stat.hospsub`
  ต่อกับ `hospcode` เอาชื่อ รพ.สต.) จัดกลุ่มตามสถานพยาบาลรองเพราะคนอ่านคือ
  เจ้าหน้าที่ที่ต้องตามเยี่ยม แต่ละคนสนใจเฉพาะรายในพื้นที่ตัวเอง
  รายที่ไม่มี `hospsub` (ราว 10%) ไม่ตกหล่น ไปรวมอยู่กลุ่ม "ไม่ระบุสถานพยาบาลรอง"
  ซึ่งยังมีที่อยู่ครบเหมือนกลุ่มอื่น

  **ไม่แจ้งคนที่เคยได้รับการวินิจฉัยความดันโลหิตสูง (ICD-10 I10)** — เป้าหมายคือ
  หาคนที่ยังไม่รู้ตัว คนที่วินิจฉัยแล้วอยู่ในระบบดูแลของคลินิกโรคเรื้อรังอยู่แล้ว
  ดูย้อนหลังทั้งประวัติทั้ง `ovstdiag` และ `iptdiag` นับ I10 กับ I109
  (I11–I15 ที่เป็นภาวะแทรกซ้อนไม่นับ) ปิดตัวกรองนี้ได้ในหน้าตั้งค่า
  ของจริงตัดจาก 96 ราย เหลือ 36 ราย ในหน้าต่าง 26 ชั่วโมงเดียวกัน

  เช็กด้วย query แยกที่ส่ง HN เป็น IN list ไม่ใช่ `EXISTS` ซ้อนในคำสั่งหลัก —
  วัดกับฐานจริงแล้วแบบซ้อนใช้ 7.4 วินาที แบบนี้ 0.1 วินาที เพราะ `ovstdiag`
  มี 3.8 ล้านแถวและ subquery แบบ correlated วิ่งใหม่ทุกแถวของผลลัพธ์

  **ที่อยู่ใส่จังหวัดเฉพาะคนต่างจังหวัด** เทียบกับจังหวัดของโรงพยาบาลเองที่อ่านจาก
  `opdconfig.hospitalcode` ไม่ฮาร์ดโค้ด — ราว 13% ของรายที่เข้าเกณฑ์เป็นคนต่างจังหวัด
  ถ้าไม่บอกจังหวัดจะตามตัวผิดที่ (มี ต.บางเลน อ.บางใหญ่ ของนนทบุรี ปนอยู่ใน
  รายงานของโรงพยาบาลในโคราช) ส่วนคนในจังหวัดเดียวกันไม่ใส่ ประหยัดความยาวข้อความ

  แต่ละรอบเก็บข้อมูล**ตั้งแต่รอบที่แล้วถึงเวลาส่ง** ไม่ใช่ตั้งแต่เที่ยงคืน —
  รายที่คัดกรองหลังเวลาส่งของวันนี้จึงไปรวมอยู่ในรอบของวันถัดไปโดยอัตโนมัติ
  ไม่มีใครหล่นหายและไม่มีใครถูกแจ้งซ้ำ

  **ส่งเป็นการ์ด Flex สีม่วง แยกข้อความตามสถานพยาบาลรอง** — รพ.สต. ที่อยู่ใน
  อำเภอเดียวกับโรงพยาบาลได้ข้อความของตัวเองแยกใบ (ของจริงมี 12 แห่ง) ส่วนคนที่
  ไม่มี `hospsub` กับคนที่ขึ้นทะเบียนไว้นอกอำเภอรวมเป็นชุดเดียวชื่อ "ผู้ป่วยรายอื่น"
  เพราะแยกไปก็ได้ใบละคนสองคนที่ไม่มีใครรับไปทำต่อ (30 วันมีสถานพยาบาลรอง
  ต่างกันถึง 145 แห่ง แต่ในอำเภอมีแค่ 12) ใช้ตัวสร้างการ์ดตัวเดียวกับตารางเวลา
  จึงได้หน้าตาชุดเดียวกันทั้งระบบ และได้ข้อความสำรองสำหรับกลุ่มที่รับการ์ดไม่ได้ฟรี

  ขึ้นการ์ดใหม่ทุก 8 ราย และซอยเป็นอีกข้อความเมื่อชุดหนึ่งเกิน 40 ราย เพราะ LINE
  จำกัด Flex ไว้ 10 KB ต่อข้อความ และ `splitPages` **ตัดที่ 5 ใบโดยโยนบล็อกที่เกิน
  ทิ้งเงียบ ๆ** — ของจริงวัดได้ใหญ่สุด 7 KB ต่อข้อความ ยังห่างเพดานพอสมควร

  ค่าที่เป็นไปไม่ได้ทางสรีรวิทยาถูกกรองทิ้งก่อนเสมอ (ค่าบนนอกช่วง 50–300
  ค่าล่างนอกช่วง 30–200 หรือค่าล่างมากกว่าค่าบน) — ในฐานจริงมีค่าพิมพ์ผิดราว
  30 แถวจาก 25,798 แถวใน 90 วัน สูงสุดที่เจอคือ 2136/874 และทุกแถวจะขึ้นไป
  อยู่บนสุดของข้อความเพราะเรียงตามค่ามาก

- **แจ้งเตือนนัดหมายล่วงหน้าผ่านหมอพร้อม** — อ่านตารางนัดจาก `oapp` ของ HOSxP
  แล้วส่งข้อความถึงผู้ป่วยก่อนถึงวันนัด ตั้งได้ว่าล่วงหน้ากี่วัน ส่งเวลาไหน
  และเปิดเฉพาะบางคลินิก พร้อมหน้าตั้งค่าที่ `/settings/appointment`
  และคำสั่ง `appointment:check` สำหรับดูข้อความก่อนเปิดใช้
  ตั้งต้นปิดและ dry run เปิด เหมือนทุกอย่างที่ส่งถึงผู้ป่วยในระบบนี้

  ใช้ MOPH Alert แบบ free form เพราะ template v3.1 มีแค่ 4 แบบและเป็นเรื่องคิวทั้งหมด
  ข้อความใช้ตัวส่งตัวเดียวกับแจ้งเตือนคิว จึงได้ dry run ช่วงงดส่ง การนับ attempt
  หน้าบันทึกการส่ง และปุ่มส่งใหม่ร่วมกัน — **ต้องมี `queue:watch` รันอยู่ด้วย**

- **ปุ่มสั่งส่งเดี๋ยวนี้** ในหน้าแจ้งเตือนนัดหมาย ทำรอบส่งทันทีโดยไม่ต้องรอเวลาที่ตั้งไว้
  ใช้ตรรกะเดียวกับรอบอัตโนมัติทุกอย่าง รวมถึง dry run การกรองคลินิก และการกันส่งซ้ำ
  กดกี่ครั้งผู้ป่วยคนเดิมก็ได้ข้อความเดียว ปุ่มเปลี่ยนสีและข้อความยืนยันระบุจำนวนคนจริง
  เมื่อ dry run ปิดอยู่ และบันทึกลงประวัติการแก้ไขว่าใครเป็นคนสั่ง


### แก้ไข

- **หน้าสถานะบอกว่าตัวเทียบฐานข้อมูลปกติ ทั้งที่ไม่ได้ทดสอบมานาน** — เพิ่มช่องเทียบฐานข้อมูล
  ในหน้า `/status` แสดงผลแยกทีละเครื่อง และขึ้นเตือน "ผลทดสอบเก่าแล้ว" ถ้าผลล่าสุดเก่ากว่า 24 ชั่วโมง
- **ค้นภาษาไทยไม่เจอ และข้อความไทยที่เขียนลงฐานมีโอกาสเพี้ยน** — MariaDB บางเครื่องตั้ง
  `init_connect = SET NAMES tis620` กับ `skip-character-set-client-handshake` ไว้ ทำให้ charset
  ที่ client ขอไม่มีผล ตอนนี้สั่ง `SET NAMES utf8mb4` เองทุกครั้งที่เปิด connection
- **`node ace` หา command ในโฟลเดอร์ `commands/` ไม่เจอบน Node 24.20 ขึ้นไป** — jsonschema 1.5.0
  resolve `$ref` แบบไม่มี base URI ไม่ได้ ตรึงไว้ที่ 1.4.1 ด้วย `overrides` ใน package.json
- **รหัสโรค 506 ในทะเบียน CDCU อ่านได้เป็นค่าว่าง** — Lucid แปลงชื่อ property `code506` เป็น
  `code_506` ซึ่งไม่มีคอลัมน์นี้ ค่าจึงเป็น undefined โดยไม่มี error ระบุชื่อคอลัมน์เองแล้ว
  (`pc_seen.icd10` ก็เจอแบบเดียวกัน)
- **วันที่เข้าทะเบียนถูกเขียนทับทุกครั้งที่แก้แถว** — MariaDB แถม `ON UPDATE current_timestamp()`
  ให้คอลัมน์ TIMESTAMP ตัวแรกที่ไม่ได้ระบุ DEFAULT migration ของรุ่นนี้ปลดออกจาก `pc_seen.created_at`
  และตารางใหม่ระบุ DEFAULT เองทุกตาราง ถ้าใครติดตั้งรุ่นก่อนหน้าแล้วรัน migration นี้ จะไม่โดน
  เพราะ migration ตรึงค่าเดิมไว้ระหว่างย้ายกลุ่มโรค


## [0.1.0] — 27 ส.ค. 2569

รุ่นแรกที่เผยแพร่ให้โรงพยาบาลอื่นนำไปใช้ได้ ระบบใช้งานจริงอยู่ที่โรงพยาบาลต้นทางมาก่อนหน้านี้แล้ว

### เพิ่ม

- **ระบบ Alert** — เฝ้าดู `ovst_queue_server` แล้วส่งแจ้งเตือนคิวถึงผู้ป่วยรายคน
  ผ่าน MOPH Alert ครบวงจร พร้อม dry run · ช่วงเวลางดส่ง · เปิดปิดรายห้อง ·
  บันทึกการส่งและปุ่มส่งใหม่
- **ระบบ Notify** — ส่งเข้ากลุ่ม LINE ของเจ้าหน้าที่ ทั้งตารางเวลาส่งซ้ำ เทมเพลตข้อความ
  และการ์ด Flex ที่แก้ได้จากหน้าเว็บโดยไม่ต้องรู้จัก schema ของ LINE
- **CDCU** — เฝ้าระวังเคส 506 จาก `surveil_member` แจ้งเข้ากลุ่ม SRRT
  ชื่อผู้ป่วย HN และเบอร์โทรเป็นตัวเลือกที่ต้องเปิดเอง ตั้งต้นปิดทั้งหมด
- **Monitor** — เทียบเครื่องฐานข้อมูลหลายเครื่องด้วย GTID และการนับแถว
  แจ้งทีมงานเมื่อ replication ตามหลัง แยกทาง หรือสายรับหยุด
- **เครื่องมือสำรวจ** — `hosxp:discover` · `hosxp:probe-queue` · `hosxp:probe-506` ·
  `hosxp:compare-hosts` สำหรับตรวจว่าฐานของโรงพยาบาลตัวเองเข้ากันได้ไหม
- **ติดตั้งด้วย image สำเร็จรูป** — `compose.prod.yml` ดึง image จาก GHCR มารันได้เลย
  ไม่ต้องมีซอร์สและไม่ต้อง build เอง อัปเดตด้วย `docker compose pull`
  ส่วนการติดตั้งจากซอร์สยังใช้ได้เหมือนเดิมสำหรับคนที่ต้องแก้โค้ด
- **ติดตั้งด้วยไฟล์อิมเมจ** — `docker save` รวม image ของระบบกับ MariaDB ไว้ในไฟล์เดียว
  (~181 MB) ปลายทาง `docker load` แล้ว `docker compose up -d` ได้เลย
  ไม่ต้องมีซอร์ส ไม่ต้อง build ไม่ต้องต่อ registry
  กรอกค่าใน `.env` แค่ 2 บรรทัดคือ `DB_PASSWORD` แล้วตามด้วย `APP_KEY`
- **CI/CD** — ตรวจ typecheck · lint · เทสต์ · build image ทุกครั้งที่ push
  และส่ง image ขึ้น registry อัตโนมัติเมื่อ tag เลขรุ่น พร้อมใบรับรองที่มาของ image
- สัญญาอนุญาต Apache-2.0

### แก้ไข

- **ล็อกอินไม่ได้เมื่อรันในโหมด production ผ่าน http://** — cookie ถูกตั้ง `Secure`
  อัตโนมัติเมื่อ `NODE_ENV=production` เบราว์เซอร์จึงไม่ส่งกลับเมื่อเข้าผ่าน
  `http://<ไอพี>:3333` บนวงแลน ผลคือล็อกอินแล้วเด้งกลับหน้าเดิมวนไปโดยไม่มี
  ข้อความบอกสาเหตุ ตอนนี้แยกเป็น `COOKIE_SECURE` ตั้งต้น `false` และเปิดเป็น `true`
  ได้เมื่อมี HTTPS อยู่หน้าระบบ (เจอตอนทดลองติดตั้งจากศูนย์ตามคู่มือ)
- ชื่อ image ตัวอย่างใน `compose.prod.yml` เป็นตัวพิมพ์ใหญ่ ซึ่ง docker ไม่ยอมรับ
  และ workflow ออกใบรับรองจะล้มถ้าชื่อบัญชี GitHub มีตัวพิมพ์ใหญ่ — แปลงเป็นตัวเล็กแล้ว
- เปิดพอร์ตหน้าเว็บผ่าน `WEB_PORT` ได้ เผื่อเครื่องนั้นมีอย่างอื่นใช้ 3333 อยู่
- จัดรูปแบบโค้ดทั้งโปรเจกต์ให้ผ่าน prettier (90 จุด) ไม่มีการเปลี่ยนพฤติกรรม

### ความปลอดภัยที่บังคับไว้ในโค้ด

- HOSxP อ่านอย่างเดียว — `HosxpClient` ปฏิเสธคำสั่งที่ไม่ใช่การอ่านตั้งแต่ในโค้ด
  และปิด `multipleStatements` ไม่ผ่าน Lucid โดยตั้งใจ
- เลขบัตรประชาชนถูกตัดทิ้งที่ชั้นล่างสุดก่อนส่งเข้ากลุ่ม LINE ทุกครั้ง
  และไม่ถูกเก็บลงฐานข้อมูลของแอป
- รหัสผ่านและ secret เข้ารหัสด้วย `APP_KEY` ก่อนลงฐาน
  ประวัติการแก้ไขแทนค่าช่องที่เป็นความลับด้วย `•••`

### หมายเหตุสำหรับผู้ที่นำไปติดตั้ง

ตารางคิวถูกกำหนดเป็น `ovst_queue_server` ตายตัวใน `queue_poller.ts`
โรงพยาบาลที่ไม่ได้ใช้ HOSxP Queue Server ต้องแก้ SQL เอง — รัน `hosxp:probe-queue` ดูก่อน
